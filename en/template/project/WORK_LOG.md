# WORK_LOG

## Why this file exists

The folder's work log. Record items that have both a performer and an artifact.

## What belongs here

- “created file X”;
- “verified that the tag matches the CHANGELOG”;
- if a decision has already been made: “recorded the standard from DECISION_LOG dated [date] in FACTS.”

Phrase each entry as an action completed and its artifact, not as a statement selecting a standard.

## What does not belong here

Decisions such as “approved,” “decided,” or “adopted.” Those belong in the decision log (`DECISION_LOG.md`).

Stop-list of verbs and phrases for this file: approved (утверждено), decided (решено), adopted (принято).

## Before you write an entry

Two questions:

1. Could the owner have chosen differently, with the choice becoming the standard going forward?
2. Does the event have a performer and an artifact (a file, a check, or a transfer)?

If only the first answer is “yes,” record the entry only in `DECISION_LOG.md`.
If only the second answer is “yes,” record the entry only in `WORK_LOG.md`.
If both answers are “yes,” write two distinct entries, not the same paragraph in both files.

---

## Example

> For the project “Publishing a training course on workspace structure”

**April 14, 2026.** Created the file `STRUCTURE_GUIDE.md`. Verified that the GUIDE lists the same folder types as the README table.
